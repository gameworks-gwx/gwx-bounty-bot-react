import * as actionTypes from 'react';
import updateObject from '../utility';

const initialState = {
  foo: ''
}

const reducer = (state = initialState, action) => {
  switch(action.type) {
    //!! Cases

    default:
      return state
  }
}

export default reducer;